<h1> Thanks for Placing the order <h1>

<% @o.each do |o|%>
	ID: <%= o.id%>
	<%= link_to "Your Order", {:action=>'show', :id=>o.id}, class:"btn btn-primary btn-lg"%>
	<%= link_to "delete", {:action=>'delete', :id=>o.id}, class:"btn btn-primary btn-lg"%>
	<%end%>